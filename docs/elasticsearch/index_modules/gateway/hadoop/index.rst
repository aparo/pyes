Hadoop HDFS Index Gateway
=========================

The Hadoop HDFS index gateway allows to store the content of a shard (actual index + transaction log) on HDFS. The path itself is derived from the node level HDFS gateway.

