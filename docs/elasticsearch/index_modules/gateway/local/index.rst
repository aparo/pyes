Local Index Gateway
===================

The local index gateway allows to store the content of a shard (actual index + transaction log) from the local data stored on each node prior to the full cluster shutdown.
